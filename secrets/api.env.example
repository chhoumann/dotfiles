# Copy to ~/.config/secrets/api.env (outside the repo) and fill in real secrets.
# Keep everything in this file exported so sourcing it populates the shell.
# export OPENAI_API_KEY="paste-openai-api-key"
# export OTHER_SERVICE_TOKEN="value"
