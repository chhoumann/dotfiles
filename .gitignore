config/zed/conversations/
config/zed/prompts/

# AI tool configs contain hardcoded auth tokens / user-local project state.
# Kept out of the repo to avoid leaking secrets and machine-local data.
config/amp/
codex/config.toml

# Machine-local brew packages (scripts/brew-drift.sh treats this as an
# overlay of the Brewfile; same syntax).
Brewfile.local

# Editor swap/backup files
*.swp
*.swo
*~
.DS_Store
